// Empty stub so vitest can load modules that import the Next.js
// `server-only` runtime guard. The real package throws at import time
// when bundled for the client; in tests we just want a no-op.
export {};
