"use client";

/**
 * A9 LISA — Local Indicators of Spatial Association.
 *
 * Wave-2 only. The registry entry for A9 has `stub: true` so the
 * `<RegistryCard>` stub branch handles the "Coming" rendering. This file
 * exists purely so the viz-registry import target resolves at module load.
 */
export function LisaMap() {
  return null;
}
