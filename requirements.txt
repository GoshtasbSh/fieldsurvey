# Vercel Python Functions — minimal runtime deps.
# Vercel reads this file (not api/requirements.txt) when building api/*.py.
# Do NOT add geopandas/fiona/shapely — they are too large for Vercel's 250MB
# function bundle limit and are only needed for local ingest. Local deps live
# in requirements-local.txt.

supabase>=2.5.0
