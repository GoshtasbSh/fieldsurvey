# Root requirements.txt — Vercel reads api/requirements.txt for functions in
# api/ (closest requirements.txt wins). This root file is effectively unused
# for the api/ functions; it exists as a project-level marker.
# Do NOT add geopandas/fiona here — they exceed Vercel's 250MB bundle limit.
# - api/requirements.txt  → packages for Vercel functions (pandas, shapely, etc.)
# - requirements-local.txt → full local dev stack (fastapi, uvicorn, geopandas)

supabase>=2.5.0
